Directory structure:
* `json_scenes/`: original scenes in JSON format
* `processed_datasets/`: processed datasets with exr, png, h5, npz files
* `templates/`: base templates for common scene objects (walls, lights)